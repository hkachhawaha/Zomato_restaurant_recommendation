The location should be a combination of dropdown and search bar. By default it should list all the locations in the dataset. The user can search for a location and the dropdown should filter the locations based on the search query. If the user searches for something else, then the output should adjust accordingly. (This change should primarily be in the front-end, but the backend should also be able to handle this)
The budget input should not be a  Low, Medium, High. It should be a user input comprising the range of budget.
Cuisine type should also be a select dropdown, rather than user guessing the options.
(Change the logic in the Prompt as well.)
No duplicate restaurants should be displayed in the results.
Ensure all the relevant match results are displayed. (By modifying the logic in the Prompt and the logic in Phase 2.)
Cuisine Type should be optional. Make the appropriate changes. (Only if it does not increase the complexity of Phase 3 and if it does, it is optional. However, do make the change in the backend though.) 
Currently, the backend is keeping Cuisine Input as a Mandatory Field. It doesn't provide the output without the Cuisine input. Make changes so that the Cuisine Input is optional. If the user doesn't provide the Cuisine Input, the backend should still provide the output. (Change the logic in the Prompt and in Phase 2's API within the /architecture.md file as well.) Run the Phase 2, once all the changes are made and check if it works.
Also, within the file, 2.Backend & Heuristic Scoring Engine (Phase 2), 4.Heuristic Scoring Math, change 'location' to 'Location'. (Do this in the @/architecture.md file in @2.set your backend prompt and do the same for the @2.py file as well.) Run the Phase 2 once all the changes are made. 
Implement appropriate functionality as per the frontend design file /2. Set Your Preferences, /1. Landing Page.js. The last option should be "More" to show all the other Cuisine options and not "Clear Selected". (Do the appropriate changes within the @/architecture.md file in @3. Frontend (Premium Next.js Portal) as well.)
When the Search results are displayed, there is an option to view more results. However, upon clicking the button, it takes me back to the Discovery Page. Instead, it should load the next 10 results on the same page. (Do the appropriate changes within the @/architecture.md file in @3. Frontend (Premium Next.js Portal) as well.) (Do the appropriate changes within the @/architecture.md file in @2.set your backend prompt and do the same for the @2.py file as well.) Run the Phase 2 and Phase 1 once all the changes are made and check if it works. (Do the appropriate changes within the @/architecture.md file in @1.ingestion.py file as well.) (Do the appropriate changes within the @/architecture.md file in @2.set your backend prompt and do the same for the @2.py file as well.) 
Within the Frontend, there are many tabs - just keep the Home and Discover tabs, remove the other tabs from the App. (Do appropriate changes within the @/architecture.md file in @3. Frontend (Premium Next.js Portal) as well.) 
Phase 2 API does provide the output when the Cuisine Input is not provided. However, it is not able to do a full fledged search and is giving very limited results. Do the appropriate changes within the Prompt as well. (Do appropriate changes within the @/architecture.md file in @2.set your backend prompt and do the same for the @2.py file as well.) Run the Phase 2 once all the changes are made and check if it works. 
There is a significant delay/latency in showing the appropriate results. Ensure that the results are shown immediately, when the user clicks on the "Find My Match" button in the "Discover" tab. In the UI, when I click on "Find My Match" button, the button is disbled and the text changes to "Finding Matches..." and when it does find the results, it should display the results immediately. Do the same for the "Chat" tab as well. (Do appropriate changes within the @/architecture.md file in @2.set your backend prompt and do the same for the @2.py file as well.) Run the Phase 2 once all the changes are made and check if it works. 

//New Changes//